
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Shared.cshtml";
}

    <fieldset>
        <div class="grid bold">
            <div class="row">
                <div class="col-3">
                    Track
                </div>
            </div>
        </div>
        <hr />
        <div class="grid">
            @foreach (var item in (Dictionary<int, string>)ViewData["routes"])
            {
            <div class="row">
                <div class="col-3">
                    @(item.Value)
                </div>

                @if (ViewData["cannotEdit" + item.Key] == null)
                {
                    <div class="col-1">
                        <a href="/Track/Edit?id=@(item.Key)">Edit...</a>
                    </div>
                }
            </div>
                    }
                    <div>
                        <a href="/Track/Create">Create New...</a>
                    </div>
                </div>
    </fieldset>

